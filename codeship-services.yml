rails:
  build:
    dockerfile_path: Dockerfile
  links:
    - redis
    - postgres
  environment:
    POSTGRES_HOST: postgres
    REDIS_HOST: redis

postgres:
  image: postgres
  volumes:
    - ./.postgres:/var/lib/postgresql/data

redis:
  build: ./redis
  env_file:
    - ./redis/redis.env
